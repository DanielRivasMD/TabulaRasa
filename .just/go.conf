####################################################################################################
# config
####################################################################################################

goapp := 'TabulaRasa'
goexe := 'tab'
dir := '.tabularasa'
cobraApp := dir / 'cobraApp'
cobraCmd := dir / 'cobraCmd'
cobraUtil := dir / 'cobraUtil'
just := dir / 'just'
readme := dir / 'readme'
todor := dir / 'todor'

####################################################################################################
