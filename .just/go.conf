####################################################################################################
# config
####################################################################################################

app := 'TabulaRasa'
exe := 'tabularasa'
dir := '.tabularasa'
cobraApp := dir / 'cobraApp'
cobraCmd := dir / 'cobraCmd'
cobraUtil := dir / 'cobraUtil'
just := dir / 'just'
todor := dir / 'todor'

####################################################################################################
